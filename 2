Write a program to store two values and to print multiplication, addition, subtraction and division.

#include <stdio.h>

int main() {
    float num1, num2;
    float sum, difference, product, quotient;

    printf("Enter the first number: ");
    scanf("%f", &num1);

    printf("Enter the second number: ");
    scanf("%f", &num2);

    sum = num1 + num2;
    difference = num1 - num2;
    product = num1 * num2;
    quotient = num1 / num2;

    printf("Addition: %.2f + %.2f = %.2f\n", num1, num2, sum);
    printf("Subtraction: %.2f - %.2f = %.2f\n", num1, num2, difference);
    printf("Multiplication: %.2f * %.2f = %.2f\n", num1, num2, product);
    printf("Division: %.2f / %.2f = %.2f\n", num1, num2, quotient);

    return 0;
}

Input:

Enter the first number: 8
Enter the second number: 2

Output:

Addition: 8.00 + 2.00 = 10.00
Subtraction: 8.00 - 2.00 = 6.00
Multiplication: 8.00 * 2.00 = 16.00
Division: 8.00 / 2.00 = 4.00
